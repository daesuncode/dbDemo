<h1>Contacts</h1>

<% people.forEach(function(contact){ %>
    <%= contact.firstName %>
    <%= contact.lastName %>
    <%= contact.age %>
    <%= contact.email %>
    <%= contact.phoneNumber %>
    <br>
<% }) %>

<a href="/people/new">Add a contact</a>